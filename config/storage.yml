test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

local:
  service: Disk
  root: <%= Rails.root.join("storage") %>

google:
  service: GCS
  project: gcp-dev-sandbox-495101
  bucket: cyrus-portfolio-prod-bucket
  iam: true
