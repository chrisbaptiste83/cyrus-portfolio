test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

local:
  service: Disk
  root: <%= Rails.root.join("storage") %>

google:
  service: GCS
  project: trinitas-forge
  bucket: cyrus-portfolio-prod-storage
  iam: true
