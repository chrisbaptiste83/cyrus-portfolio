# frozen_string_literal: true

# CORS is intentionally omitted.
#
# This application serves same-origin requests via Inertia.js. No cross-origin
# browser access is required, so the rack-cors gem is not included and no
# CORS policy is configured.
